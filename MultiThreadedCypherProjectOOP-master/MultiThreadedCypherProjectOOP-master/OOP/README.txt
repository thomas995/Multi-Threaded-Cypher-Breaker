This is my Multi Threaded Cypher breaker project for Object Orented Programming.

A text file called message.txt is read in as an input and the railfence class decrypts it to plain text.

The ant builder must built before execution. However an error can occur and if this build fails you might have to change the system library to JDK and not JRE.